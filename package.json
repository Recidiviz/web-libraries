{
  "name": "@recidiviz/libraries",
  "version": "0.0.0",
  "description": "Packages shared across the Recidiviz web platform",
  "author": "Recidiviz <team@recidiviz.org>",
  "license": "GPL-3.0-only",
  "private": true,
  "scripts": {
    "postinstall": "run-p install:*",
    "install:design-system": "(cd packages/design-system && yarn install)",
    "install:eslint-config": "(cd packages/eslint-config && yarn install)",
    "install:tsconfig": "(cd packages/tsconfig && yarn install)"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
